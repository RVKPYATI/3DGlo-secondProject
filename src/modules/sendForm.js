export const sendForm = (idForm) => {
    const form = document.querySelector(idForm);
    
    form.addEventListener('submit', (e) => {
        e.preventDefault();
        console.log('dfd')
    });



















};